async function run() {
  // Let's start the Next.js server first? Or maybe we can just write a quick script to test the route logic directly
  // Actually, I can't start the server if it's already running.
  // The user says "data 's are not getting visible. fix this." 
  // Maybe I can just import GET from the route and call it.
}
